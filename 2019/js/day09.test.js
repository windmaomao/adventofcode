import { Intcode } from './day09'
// import read from './read'

describe('2019 Day 09', () => {

  // const ops = read('5')[0].split(',').map(Number)
  const example1 = [109,1,204,-1,1001,100,1,100,1008,100,16,101,1006,101,0,99]

  it('part1 example', () => {
    // expect(Intcode(example1).run()).toEqual(0)
  })

})
