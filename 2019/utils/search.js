
const bfs = (start, neighbours, goal) {
  const current_gen = [start]
  const prev = { start: null }
  const goals = []
  let gen = -1

  while (current_gen.length) {
    gen++
    const next_gen = []
    
  }
}

const search = {
  bfs  
}

module.exports = search