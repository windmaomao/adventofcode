import { extractOp, part1 } from './day06'
import read from './read'

describe('2015 Day 06', () => {

  const ops = read('06').map(extractOp)
// console.log(ops)
  // it('part1', () => {
  //   expect(part1(ops)).toEqual(0)
  // })


})
